# Swagger UI — Руководство по использованию

> 📘 **Навигация:** [Что такое](#что-такое-swagger-ui) | [Как открыть](#как-открыть-swagger-ui) | [Эндпоинты](#доступные-эндпоинты) | [Авторизация](#авторизация-в-swagger-ui)

---

## Что такое Swagger UI?

**Swagger UI** — это интерактивный интерфейс для документирования и тестирования REST API. Он позволяет:

- 📖 **Изучать документацию** — все эндпоинты с описаниями, параметрами и примерами
- 🧪 **Тестировать API** — отправлять запросы прямо из браузера без Postman/cURL
- 🔐 **Управлять авторизацией** — сохранять JWT токен для защищённых эндпоинтов
- 📥 **Экспортировать спецификацию** — скачать OpenAPI документ в формате YAML

### Архитектура API

```
┌─────────────────────────────────────────────────────────────────┐
│                    🌐 Клиентская сторона                        │
│                    🖥️ Браузер                                   │
│              http://localhost:3000/docs                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ 1. Открывает docs
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    📡 Backend (NestJS)                          │
│  ┌─────────────┐  ┌──────────────┐  ┌─────────────┐             │
│  │ 📄 Swagger  │→ │ 🎯 Controllers│→ │ 🔐 JWT Guard│           │
│  │    UI       │  │ auth.controller│ │ Проверка    │            │
│  │             │  │ user.controller│ │ токенов     │            │
│  └─────────────┘  └──────────────┘  └─────────────┘             │
│                          │                    │                 │
│                          ▼                    │                 │
│                   ┌──────────────┐            │                 │
│                   │ ⚙️ Services  │←───────────┘                 │
│                   │ auth.service │                              │
│                   │ user.service │                              │
│                   └──────────────┘                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ 5. Запрос данных
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    💾 Данные                                    │
│         ┌──────────────┐         ┌──────────────┐              │
│         │ 🗄️ PostgreSQL│         │ 🍪 Cookies   │              │
│         │ localhost:5433│        │ Refresh токен│              │
│         └──────────────┘         └──────────────┘              │
└─────────────────────────────────────────────────────────────────┘
```

> 💡 **Как читать схему:** Поток идёт сверху вниз — от браузера через Backend к базе данных и обратно.

---

## Интерфейс Swagger UI

> 💡 **Подсказка:** Все эндпоинты можно сворачивать/разворачивать кликом на названии метода.

### Основные элементы

```
┌────────────────────────────────────────────────────────────────┐
│  MeowVault                              [Authorize 🔑]         │
│  API документация                                              │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ▼ Auth                                                       │
│    ┌─────────────────────────────────────────────────────┐    │
│    │ 🟢 POST /auth/register    Регистрация пользователя  │    │
│    │ 🟢 POST /auth/login         Вход в систему          │    │
│    │ 🟢 POST /auth/refresh       Обновление токенов      │    │
│    │ 🟢 POST /auth/logout        Выход из системы        │    │
│    └─────────────────────────────────────────────────────┘    │
│                                                                │
│  ▼ User                                                       │
│    ┌─────────────────────────────────────────────────────┐    │
│    │ 🟢 GET  /user               Данные пользователя     │    │
│    │ 🟡 PATCH /user/profile      Обновление профиля      │    │
│    │ 🟡 PATCH /user/avatar       Обновление аватара      │    │
│    │ 🟡 PATCH /user/password     Смена пароля            │    │
│    │ 🔴 DELETE /user             Удаление аккаунта       │    │
│    └─────────────────────────────────────────────────────┘    │
│                                                                │
└────────────────────────────────────────────────────────────────┘

🟢 GET  — Получение данных
🔵 POST  — Создание/отправка данных
🟡 PATCH — Частичное обновление
🔴 DELETE — Удаление
```

| Элемент              | Описание                                            |
| -------------------- | --------------------------------------------------- |
| **Название API**     | Вверху страницы — название проекта (MeowVault)      |
| **Секции**           | Эндпоинты сгруппированы по модулям: `Auth`, `User`  |
| **Методы**           | Цветные метки: 🟢 GET, 🔵 POST, 🟡 PATCH, 🔴 DELETE |
| **Кнопка Authorize** | 🔑 Вверху справа — для ввода JWT токена             |
| **Try it out**       | Кнопка для активации формы запроса                  |

---

## Как использовать Swagger UI для тестирования

### Пошаговая инструкция

```
Шаг 1: Раскройте эндпоинт (клик на названии)
         │
         ▼
Шаг 2: Нажмите кнопку "Try it out"
         │
         ▼
Шаг 3: Введите данные в форму запроса
         │
         ▼
Шаг 4: Нажмите кнопку "Execute"
         │
         ▼
Шаг 5: Просмотрите ответ (статус, заголовки, тело)
```

### Пример: Регистрация пользователя

1. Найдите секцию **Auth** → `POST /auth/register`
2. Нажмите **Try it out**
3. Вставьте JSON в поле **Request body**:
   ```json
   {
     "email": "test@example.com",
     "username": "test_user",
     "password": "TestPass123!"
   }
   ```
4. Нажмите **Execute**
5. В разделе **Responses** увидите:
   - Status: `201`
   - Response body: `{ "accessToken": "..." }`
6. Скопируйте токен для авторизации

---

## Авторизация в Swagger UI

Большинство эндпоинтов требуют аутентификации через JWT токен.

### Схема аутентификации

```
┌─────────┐     ┌─────────┐     ┌──────────────┐     ┌──────────┐
│Пользова-│     │ Swagger │     │ POST /auth/  │     │ База     │
│тель     │     │ UI      │     │ login        │     │ данных   │
└────┬────┘     └────┬────┘     └──────┬───────┘     └────┬─────┘
     │               │                  │                  │
     │ Ввести логин/ │                  │                  │
     │ пароль        │                  │                  │
     │──────────────→│                  │                  │
     │               │ POST /auth/login │                  │
     │               │─────────────────→│                  │
     │               │                  │ Найти пользователя│
     │               │                  │─────────────────→│
     │               │                  │←─────────────────│
     │               │                  │ { email, password }
     │               │                  │                  │
     │               │                  │ Проверка пароля  │
     │               │                  │ Генерация JWT    │
     │               │←─────────────────│                  │
     │               │ { accessToken }  │                  │
     │←──────────────│                  │                  │
     │ { accessToken }                   │                  │
     │               │                  │                  │
     │ Нажать        │                  │                  │
     │ Authorize 🔑  │                  │                  │
     │──────────────→│                  │                  │
     │               │ GET /user +      │                  │
     │               │ Bearer токен     │                  │
     │               │─────────────────→│                  │
     │←──────────────│                  │                  │
     │ Данные профиля│                  │                  │
     │               │                  │                  │
└────┴────┘     └────┴────┘     └──────────────┘     └──────────┘
```

### Как авторизоваться:

1. Нажмите кнопку **Authorize** 🔑 (вверху справа)
2. В поле введите токен в формате:
   ```
   Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   ```
3. Нажмите **Authorize**
4. Нажмите **Close**

> ✅ После авторизации токен сохраняется в браузере и используется для всех защищённых запросов.

### Как получить токен:

1. Выполните запрос `POST /auth/login` или `POST /auth/register`
2. Скопируйте значение `accessToken` из ответа
3. Используйте его для авторизации

---

### Обзор модулей

```
┌─────────────────────────────────────────────────────────────────┐
│                     🔓 Auth Module                              │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ POST /auth/register   │ Регистрация нового пользователя │   │
│  │ POST /auth/login      │ Вход в систему                  │   │
│  │ POST /auth/refresh    │ Обновление токенов              │   │
│  │ POST /auth/logout     │ Выход из системы                │   │
│  └─────────────────────────────────────────────────────────┘   │
│  → Публичные эндпоинты (не требуют авторизации)                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                     🔐 User Module                              │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ GET  /user            │ Получение данных пользователя   │   │
│  │ PATCH /user/profile   │ Обновление профиля              │   │
│  │ PATCH /user/avatar    │ Обновление аватара              │   │
│  │ PATCH /user/password  │ Смена пароля                    │   │
│  │ DELETE /user          │ Удаление аккаунта               │   │
│  └─────────────────────────────────────────────────────────┘   │
│  → Защищённые эндпоинты (требуют JWT токен)                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## Структура проекта

```
src/modules/
│
├── auth/
│   ├── auth.controller.ts      # Эндпоинты /auth/*
│   ├── auth.service.ts         # Бизнес-логика аутентификации
│   └── dto/
│       ├── create-auth.dto.ts  # DTO для регистрации
│       └── login-auth.dto.ts   # DTO для входа
│
└── user/
    ├── user.controller.ts      # Эндпоинты /user/*
    ├── user.service.ts         # Бизнес-логика пользователя
    └── dto/
        ├── update-user.dto.ts      # DTO для обновления профиля
        └── update-user-pass.ts     # DTO для смены пароля
```

---

## Поддержка

При возникновении проблем:

1. Проверьте, что сервер запущен (`npm run start:dev`)
2. Убедитесь, что порт 3000 не занят другими приложениями
3. Проверьте логи сервера на наличие ошибок
4. Убедитесь, что JWT токен действителен и не истёк

---

> 📖 **Версия документа:** 1.0 | **Последнее обновление:** Март 2026
