# Дата: 2026-03-24

Работала с PR исправляла замечания, вполне успешно, и не так все критично для первой работы с БЭКом, что не может не радовать. Что исправлено:

### 🔴 Критические исправления

1. Публичный endpoint без авторизации

Удалён декоратор `@Public()`, который отключал JWT-защиту
Эндпоинт больше не доступен без авторизации
Потенциальная проблема с rate limiting остаётся - будет решаться глобально Алексом

Вывод:
Без защиты такие эндпоинты могут привести к неконтролируемым расходам внешнего API

2. Неправильная обработка response

Изменён порядок:
сначала response.ok
потом response.json()

Вывод:
Парсинг ответа до проверки статуса - частая ошибка, ведущая к runtime падениям

### 🟡 Средние исправления

3. Удалены `console.log`

4. Обработка `JSON.parse`. Добавлена проверка на пустую строку перед парсингом (AI может вернуть невалидный JSON)

5. Убран лишний spread:  `return { ...message }` → `return message`

6. DTO: ограничение personality: добавлен `@IsEnum()`

7. `AiModule` перемещён в `games/merge-game`. Добавлен в exports


### 🔵 Низкий приоритет

8. Исправлены опечатки в Swagger
9. Переименованы интерфейсы (Grok → Groq)
10. Перенесены константы в `constants/`
11. Исправлено именование файлов


### ⏳ Отложено

Обработка ошибок в `catch`

Сейчас все ошибки приводятся к `BadRequestException`
Подсказки от ИИ:
логирование через Logger
разделение типов ошибок (400 vs 503)



