Дата 2026-03-29

Последняя неделя разработки фичей, и последняя неделя дневников.

Решил попробовать написать одну страницу на фронте использую ангуляр, я сделал темплейт компонент карточек и страницу about. Так же сделал массив информации о каждом участнике нашей команды, и потом, из компонента about вызвать массив и циклом создать карточки, я вначале не понял как передавать информации в дочернии компоненты, мне вначале рассказали про старый вариант прокидки инфы через дерективы input. Не очень удобно. Далее научили пользоваться сигналами, их использовать приятнее и проще чем старый вариант через дерективы.

Я написал основу карточек и далее, задал ии промт создать мне стили использую как пример весь проект на ангуляре. Дизайн с первого раза оказался очень хорошим, потому я его и оставил.

Далее, я решил попробовать дать ии задание прочитать все дневники и так же всю историю коммитов и сделать документ, который будем использовать дальше для заполнение информации массива.

ИИ не совсем верно описала достижения и какие компоненты написала наша команда, и я вручную правил документ.

И последний штрих, заполнил карточки и с помощью того же ии сделал переводы на все карточки и так же дал ему задание из тех же дневников создать карточки ии которые использовали в проекте.

[PR#193](https://github.com/ngKittyDebug/RS-Tandem-ngKittyDebug/pull/193)

Круто, ии сократила мне работу с пару дней до одного.


Далее на бэке, я реализовал новые таблички со статистикой. Мыы решили не что не успем добавить ачикви  и много статистики, потому реализовал самые простое:

Это количество созданных игр 
И последняя игра в которую играл

реализовал связи один ко многим, профиль юзера связана с табличками игр, сами таблички создаются только когда в первый раз обращаться с квери запросом к игре.

а дальше, как одет обращение к той же игре, просто увеличивается счетчик на один и обновляется строка updatedAt

Самое простая реализация и самая быстрая, а так же можно ее расширить при необходимости.

Была небольшая проблема когда нужно было переименовать поле в модели, и не работали миграции, потому попросил ии показать как делать такой  сырой запрос к табличкам через ту же самую миграцию

```sql
ALTER TABLE "game_statistic" 
  ALTER COLUMN "gameType" 
  TYPE "GameType_new" 
  USING (
    CASE 
      WHEN "gameType"::text = 'MeowVentLoop' THEN 'eventLoop'::"GameType_new"
      ELSE "gameType"::text::"GameType_new"
    END
  );

ALTER TYPE "GameType" RENAME TO "GameType_old";
ALTER TYPE "GameType_new" RENAME TO "GameType";

DROP TYPE "public"."GameType_old";
```

Так же появился баг с github OAuth, как мы его решили думаю напишу завтра.

И это все что я пока вспомнил

посмотрим сможем мы ли решить проблему с гитом