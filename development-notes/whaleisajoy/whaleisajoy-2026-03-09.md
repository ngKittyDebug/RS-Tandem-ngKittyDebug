#Дата: 2026-03-09

**Что было сделано**:
Я смогла привести таки страницу User profile в нормальный вид. Но после представления странички команде, было решено немного изменить блок с настройками, чтобы сразу было видно, что можно изменить и username, email и пароль. Сначала было сделано, что изменение пароля появляется по кнопке редактировать профиль.

Теперь еще нужно настроить подтягивание данных юзера с бэка. Для этого создам User service, который будет предоставлять методы для работы с данными. Я его уже создала, протестировала получение пользователя. Работает. Но нужно еще добавить хранение токена в localStorage. Без токена ответ от сервера не получить. Но радует, что хоть что то уже работает.

Еще сначала делала всю верстку на странице, чтобы просто понять как работать с тайгой. Потом пришло таки время разделять ~~и властвовать~~. Разделила компоненты. Подумала, что тогда можно и переводы разделить по папкам. Сейчас все лежит в папке i18n, а в ней папки по страницам, а там уже два файла с переводами. Создала внутри папки со своей страницей еще одну с компонентом. И тут то и выяснилось, что так не работает. Созванивались с командой, нашли пару решений.

1. Хранить такие переводы прямо внутри компонентов, к которым они относятся.  
   **Минусы:** если бы добавили новый язык, то пришлось бы по всем папкам идти и менять. Так что логичнее, чтобы все переводы лежали в одном месте.

2. Добавлять `scope` для вложенных папок в `app.routes`.  
   Выглядит так себе, несколько захламляет этот файл. Возможно, будем использовать этот вариант для наших игр.

Начала продумывать логику для своей игры. И поняла, что все не так просто как я думала (вот сюрприз). Пользователю нужно будет переносить плашки по microtask, macrotask и stack. И еще и показывать, что выведется в консоль. Получается, нужно показывать:

- весь код для удобства;
- перетаскиваемые плашки с кодом;
- области с тасками;
- вывод в консоль с сортировкой, я думаю.

Все это уместить в один экран будет проблематично. Видимо, нужно будет сделать выполнение по шагам.

А вот и ссылочка на видео про мой компонент: [link](https://youtu.be/eh0Jf0RiPNs)

**Планы**:

- Создать User service
- Доделать User profile
- Продумать логику и дизайн для игры
