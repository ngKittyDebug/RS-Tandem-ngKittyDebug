#Дата: 2026-03-13

**Что было сделано**:
Итак, работа над User profile продрожается. У нас добавился интерсептор, который автоматически будет подставлять токен в запросы. Значит, мой userService станет совсем простым, класс.

Приступила к разработке userStore для хранения данных пользователя. Сделала его на сигналах, потому что в полноценный rxjs лезть не хочется. Да и в нашем проекте он и не нужен скорее всего. А вот использовать данные из стора в компонентах оказалось не так то и просто. Ведь данные загружаюься не сразу. И надо делать проверки. Если данных еще нет -> показывай прелоадер. У Taiga UI есть свой skeleton, который отображается в таком случае. Прикольно, но надо ставить его на каждый компонент. А есть вариант просто сделать отдельный компонент лоадера с котиком, который будет запускаться, пока данные не получены. Этот вариант сильно проще. Всего одна проверка и отображение одного компонента. Еще у меня есть форма с данными пользователя, где они должны быть заполнены изнчально. И вот просто через сигнал из userStore это уже не сработает. Потому что надо патчить форму и подставлять данные в нее. Для этого используется эффект, который отслеживает изменение юзера. А еще надо будет написать тесты на это все.

Сделали еще одну попытку настроить environments в нашем проекте. Ведь angular не умеет работать со стандартным .env. А у нас частенько меняется адрес сервера и фалйы все время перезаписываются и попадают в гит. Не очень. В ангуляр можно сгенерировать свои environments. Но! Переменные из .env туда все равно не подставить. Так что этот вариант пока отпал.

Параллельно немного настраивала свою игру. Поняла, что для начала сделаю какую то минимальную реализацию, которую уже потом буду доделывать. Поэтому изначально у меня будет блок с кодом. Ниже console output с уже добавленными опциями. Они будут sortable, то есть их надо будет поставить в верном порядке. По кнопке ответ будет проверяться и показываться обратная связь с анимацией. Потом хочу еще добавить уровни сложности, где уровень задачек будет усложняться.

**Планы**:

- Доделать userSore
- Доделать userProfile до какого то минимума, который можно запушить
- Написать тесты для компонентов
