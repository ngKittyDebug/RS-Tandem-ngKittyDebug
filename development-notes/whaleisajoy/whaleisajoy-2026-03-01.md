#Дата: 2026-03-01

**Что было сделано**:
Первый день весны — работа продолжается 🌸

На днях поняли, что акцентную кнопку надо менять.

**Проблема:** Кнопка была настроена кастомным классом без Taiga UI, с захардкоженными размерами. А размеры кнопки нужно будет менять. Да и сама идея использовать специальную библиотеку для UI и при этом иметь свою полностью кастомную кнопку выглядит так себе.

**Решение:** Стала разбираться с Taiga UI. Там есть `appearance`, и выглядит так, что это как раз то, что нужно. Так и оказалось.Разобралась, как с этим работать. В итоге закастомила именно `TuiButton` с новым `appearance="gradient"`.

Пример:

```html
<button tuiButton appearance="gradient" size="m">Click me</button>
```

Поменяла буквально пару свойств — и вуаля, у нас тайговская кнопка с кастомным видом.
При этом можно использовать все остальные плюшки Taiga UI.

Закинула PR, его апрувнули, и я всё замержила.
Теперь все имеют доступ к красивой кнопочке ✨
Добавила об этом информацию в наш Wiki.

---

Ещё на этой неделе был первый PR с готовым компонентом: `header` с переключением темы и языка.
Провели ревью, оставили комментарии, что можно поправить. Я тоже нашла, что можно улучшить. Правки учли, ветку замержили.
И вот у нас уже есть хедер и логика переключения языка, которую нужно будет всем использовать и дописывать в своих компонентах.

---

Еще наш доблестный бэкендер сделал нам CI/CD. Для нетлифая нужно было написать мини конфиг, чтобы корректно работал роутинг в SPA. Плюс надо было учесть, что у нас монорепо. В общем, покопавшись в доках, написала конфиг. И он даже сразу заработал. Шок.

---

Кстати, мы же меняли название нашего приложения. Раньше было название команды, а теперь это MeowVault. Так что понадобилось нарисовать еще и лого для аппки. А предыдущее лого с командой я решила поставить на аватарку нашей организации. Попросила иишку только перерисовать немного лого, что оно было чуть более вертикальным. Получилось красиво.

---

Провели ещё один общий мит. На нём поделились своими проблемами, обсудили вопросы. Они касались:

- работы с Angular
- Taiga UI
- цветов приложения

С этим всем благополучно разобрались. Дополняем Wiki инструкциями по необходимости.

---

Ну пора бы и свой компонент начинать, верно? Так и поступила. У меня это User profile. Стала просто для примера накидывать комопненты тайги, типо карточек, и смотреть как это будет выглядеть. А после того, как стянула изменения с языком, оказалось, что на моей страничке переключение не работает. На `main`, например, работает, а у меня нет. Стала разбираться.

**Проблема с Transloco**

Дело оказалось в кейсе. На каждый страничке сейчас подключен provideTranslocoScope. У меня было так:

```ts
provideTranslocoScope('user-profile');
```

А в html компонента используется:

```html
<p *transloco="let t">{{ t('user-profile.userProfileWorks') }}</p>
```

Оказалось, что Transloco конвертирует `scope` в `camelCase`.
Поэтому нужно использовать:

```html
<p *transloco="let t">{{ t('userPofile.userProfileWorks') }}</p>
```

Вот так починила этот моментик.

Первый раз написала так много всего в дневнике.

**Планы**:

- Продолжить работу над User profile.
- Еще надо будет добавить переменные с размерами под разные устройства, чтобы удобно делать адптив.
- Добавить coverage для тестов.
