export enum ThemeNames {
  Light = 'light',
  Dark = 'dark',
}

export enum ErrorTosterLabels {
  Error = 'error',
  Warning = 'warning',
  Positive = 'meow',
}

export enum ErrorTosterAppearances {
  Error = 'negative',
  Warning = 'warning',
  Positive = 'positive',
}
